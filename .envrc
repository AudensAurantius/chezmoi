export GITHUB_USER=AudensAurantius
export GITHUB_TOKEN="$(pass show professional/Bold_Orange/github/tokens/Dell_XPS)"

# bd-timew: beads dolt server — managed by bd-timew init-project
# Re-run bd-timew init-project --server to update.
bd dolt status &>/dev/null || bd dolt start
export BEADS_DOLT_PASSWORD="$(pass show personal/chezmoi/beads | head -1)"
