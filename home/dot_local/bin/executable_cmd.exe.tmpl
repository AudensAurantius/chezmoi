{{- if eq .chezmoi.os "linux" -}}
{{- if (.chezmoi.kernel.osrelease | lower | contains "wsl" ) -}}
#!/bin/sh
# Wrapper around Microsoft cmd.exe
# This makes it possible to call cmd.exe without adding /mnt/c/Windows/system32 to PATH

exec /mnt/c/Windows/system32/cmd.exe "$@"
{{- end }}
{{- end }}
