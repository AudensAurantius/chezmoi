{{- if eq .chezmoi.os "linux" -}}
{{- if (.chezmoi.kernel.osrelease | lower | contains "wsl" ) -}}
#!/bin/sh
# Wrapper around Microsoft explorer.exe
# This makes it possible to call explorer.exe without adding /mnt/c/Windows to PATH

/mnt/c/Windows/explorer.exe "$@"
{{- end }}
{{- end }}
